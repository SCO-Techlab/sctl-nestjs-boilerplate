import { MAGIC_STRINGS } from "@shared/constants";

export const PERMISSIONS = {
  PERMISSIONS: MAGIC_STRINGS.PERMISSIONS.toUpperCase(),
  ROLES: MAGIC_STRINGS.ROLES.toUpperCase(),
  USERS: MAGIC_STRINGS.USERS.toUpperCase()
};